# 🔴 Система Параноидального Логирования и Отладки

## Обзор

Система логирования полностью интегрирована в приложение ZONT Spec Extractor. Она обеспечивает **тотальное логирование** всех операций без изменения бизнес-логики приложения.

## Компоненты

### 1. **LoggingSystem.cs** - Базовая система логирования
- Уровни логирования: DEBUG, INFO, WARNING, ERROR, CRITICAL
- Асинхронная обработка логов
- Автосохранение в файлы
- Фильтрация и поиск

### 2. **TelegramLogger.cs** - Интеграция с Telegram
- Асинхронная отправка логов уровня WARNING и выше
- Форматирование сообщений в HTML
- Автоматическое разбиение длинных сообщений

### 3. **DebugPanelForm.cs** - Панель управления "КРАСНАЯ КНОПКА"
- 12 функций управления
- Логи в реальном времени
- Мониторинг памяти и CPU

### 4. **LogMethodAttribute.cs** - Автоматическое логирование методов
- Атрибуты для методов
- Обертки для безопасного выполнения

## Настройка

### 1. Настройка Telegram (опционально)

Установите переменные окружения:

**Windows (PowerShell):**
```powershell
$env:TELEGRAM_BOT_TOKEN = "YOUR_BOT_TOKEN"
$env:TELEGRAM_CHAT_ID = "YOUR_CHAT_ID"
```

**Windows (CMD):**
```cmd
set TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN
set TELEGRAM_CHAT_ID=YOUR_CHAT_ID
```

**Через системные переменные:**
1. Панель управления → Система → Дополнительные параметры системы
2. Переменные среды → Новый
3. Добавьте `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`

### 2. Получение Telegram Bot Token

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

### 3. Получение Chat ID

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Бот покажет ваш Chat ID
3. Или создайте группу и добавьте [@RawDataBot](https://t.me/RawDataBot) - он покажет ID группы

## Использование

### Автоматическое логирование

Система автоматически логирует:
- Вход/выход из методов (через `MethodLogger.LogEntry/LogExit`)
- Все исключения (через `LoggingSystem.LogException`)
- Ключевые операции в `Form1.cs`

### Ручное логирование

```csharp
// Простое логирование
LoggingSystem.Log(LogLevel.INFO, "ClassName", "MethodName", "Message");

// С параметрами
var params = new Dictionary<string, object> { { "param1", value1 } };
LoggingSystem.Log(LogLevel.DEBUG, "ClassName", "MethodName", "Message", null, params);

// Логирование исключений
LoggingSystem.LogException("ClassName", "MethodName", exception);
```

### Использование оберток

```csharp
// Для методов с возвращаемым значением
var result = MethodLogger.ExecuteWithLogging(() => {
    // Ваш код
    return someValue;
});

// Для void методов
MethodLogger.ExecuteWithLogging(() => {
    // Ваш код
});
```

## Панель управления "КРАСНАЯ КНОПКА"

Панель автоматически запускается при старте приложения в отдельном окне.

### Функции панели:

1. **🔴 PANIC BUTTON**
   - Принудительная остановка
   - Дамп состояния памяти
   - Сохранение всех логов
   - Отправка в Telegram

2. **📤 Send Log to TG**
   - Отправка текущего лога в Telegram

3. **💾 Save to PC**
   - Сохранение лога в файл (.log)

4. **🔄 Auto-Save Toggle**
   - Включение/выключение автосохранения
   - Настройка интервала (10-3600 секунд)

5. **🧹 Clear Console**
   - Очистка окна логов

6. **🔍 Search/Filter**
   - Поиск по ключевым словам
   - Фильтрация в реальном времени

7. **⏸ Pause Logging**
   - Временная приостановка вывода логов

8. **📋 Copy to Clipboard**
   - Копирование всего лога в буфер обмена

9. **📉 Memory Usage**
   - Отображение потребления памяти и CPU (обновляется каждые 2 секунды)

10. **📧 Email Report**
    - Заглушка для будущей реализации

11. **🎨 Color Mode**
    - Переключение цветовой подсветки
    - Ошибки - красным, предупреждения - желтым, инфо - зеленым

12. **🐛 Inject Test Error**
    - Искусственное создание ошибки для тестирования системы

## Расположение логов

Логи сохраняются в:
```
%USERPROFILE%\Documents\ZontSpecExtractor_Logs\
```

Формат имени файла: `log_YYYY-MM-DD_HH-mm-ss.log`

## Уровни логирования

- **DEBUG** - Детальная отладочная информация
- **INFO** - Общая информация о работе
- **WARNING** - Предупреждения (отправляются в Telegram)
- **ERROR** - Ошибки (отправляются в Telegram)
- **CRITICAL** - Критические ошибки (отправляются в Telegram)

## Безопасность

- Система логирования **не может** уронить основное приложение
- Все ошибки в обработчиках логов перехватываются
- Debug панель работает в отдельном потоке
- При сбое Telegram отправки приложение продолжает работу

## Производительность

- Логирование асинхронное (не блокирует UI)
- Очередь логов обрабатывается в фоне
- Минимальное влияние на производительность
- Автоматическая очистка буфера при сохранении

## Расширение системы

### Добавление нового обработчика логов

```csharp
public class CustomLogHandler : ILogHandler
{
    public void Handle(LogEntry entry)
    {
        // Ваша логика обработки
    }
}

// Регистрация
LoggingSystem.AddHandler(new CustomLogHandler());
```

### Использование атрибутов

```csharp
[LogMethod(MinLevel = LogLevel.INFO, LogParameters = true)]
private void MyMethod(string param1, int param2)
{
    // Метод будет автоматически логироваться
}
```

## Устранение неполадок

### Telegram не отправляет логи
1. Проверьте переменные окружения `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
2. Убедитесь, что бот запущен
3. Проверьте интернет-соединение
4. Логи уровня ниже WARNING не отправляются

### Debug панель не открывается
1. Проверьте, нет ли ошибок в консоли
2. Убедитесь, что приложение запущено с правами администратора (если требуется)
3. Проверьте логи в файле

### Высокое потребление памяти
1. Включите автосохранение с меньшим интервалом
2. Очищайте консоль периодически
3. Используйте фильтрацию для уменьшения объема логов

## Технические детали

- **Потокобезопасность**: Все операции с логами потокобезопасны
- **Производительность**: Асинхронная обработка, минимальные блокировки
- **Надежность**: Обработка всех исключений, graceful degradation
- **Масштабируемость**: Легко добавлять новые обработчики

## Лицензия

Система логирования интегрирована в существующий проект и следует его лицензии.


